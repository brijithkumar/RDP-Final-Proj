import { ProjectTaskFilterPipe } from './project-task-filter.pipe';

describe('ProjectTaskFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ProjectTaskFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
