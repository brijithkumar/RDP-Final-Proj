import { ManagerFilterPipe } from './manager-filter.pipe';

describe('ManagerFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ManagerFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
